import MarvelRivalsPageClient from "@/components/game/MarvelRivalsPageClient";

export const metadata = {
  title: "Marvel Rivals — KidStore",
  description: "Lattice para Marvel Rivals con entrega por UID. Sin acceso a tu cuenta.",
};

export default function MarvelRivalsPage() {
  return <MarvelRivalsPageClient />;
}
